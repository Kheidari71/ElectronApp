import Header from '../layout/header/Header';
import Login from '../login/Login';

const Authorize = ({ setLogIn }) => {

    return (

        <Login setLogIn={setLogIn} />

    );
}

export default Authorize;
